# Read the guidelines for editing:
# https://github.com/ilo-token/ilo-token.github.io/wiki/Guidelines-for-editing-dictionary

# The dictionary is licensed under:
# Creative Commons Attribution-ShareAlike 4.0 International
# https://creativecommons.org/licenses/by-sa/4.0/

# Many definitions are adapted from Linku (https://linku.la).
# sona Linku is dual-licensed under:
# - Creative Commons Attribution-ShareAlike 3.0 Unported
#   https://github.com/lipu-linku/sona/blob/main/LICENSE.CC-BY-SA-3.0
# - Creative Commons Attribution-ShareAlike 4.0 International
#   https://github.com/lipu-linku/sona/blob/main/LICENSE.CC-BY-SA-4.0

a:
    # core

    ah/aah/aaah(f);
    # oh/ohh/ohhh(f);
    # ha/haa/haaa(f);
    # eh/ehh/ehhh(f);
    # um/umm/ummm(f);
    # oy/oyy/oyyy(f);

    `[`placed after something for emphasis or emotion`]`(particle def);

akesi:
    # core

    reptile(n);
    amphibian(n);
    scaly(adj material) creature(n);
    crawling(adj opinion gerund-like) creature(n);

    reptilian(adj qualifier);
    amphibian(adj qualifier);

ala:
    # core

    not(d negative);
    not(adv negative);
    nothing(n plural);
    none(n plural);
    no(d quantifier plural);

    `[`negates a word or phrase`]`(particle def);
    `[`forms a yes-no question`]`(particle def);

    # 0(num);
        # While technically true, this just floods the output. ilo Token
        # prioritizes numerals at the moment which is bad because normal
        # negation are more interesting. Thanks soweli Eweke for pointing this
        # out!

alasa:
    # core

    # hunt(v) [object];
    search(v) for(prep) [object];

    try to(v) [predicate];

    # hunting(n gerund);
    searching(n gerund);

    # hunting(adj qualifier gerund-like);
    searching(adj qualifier gerund-like);

ale, ali:
    # core, uncommon

    all(d quantifier plural);
    # every(d distributive);
    everything(n singular);
    # entirety(n singular);
    universe(n singular);

    100(num);

    completely(adv);

anpa:
    # core

    # bottom(n singular);
    underside(n singular);

    defeated(adj opinion);
    humble(adj opinion);
    lowly(adj opinion);

    defeat(v) [object];

ante:
    # core

    different(adj opinion);
    # altered(adj opinion);

    # alter(v) [object];
    # modify(v) [object];
    change(v) [object];

    other(adj origin);
    other(n);

    difference(n);

    differently(adv);

anu:
    # core

    `[`separates multiple possibilities, replacing another particle`]`
        (particle def);
    or(particle def);

awen:
    # core

    # stay(v);
    remain(v linking) [predicate];
        # Thanks to ilo Tani for telling me this is supposed to be a linking
        # verb
    # wait(v);
    pause(v) [object];

    protect(v) [object];
    # keep safe(v);

    continue to(v) [predicate];

    # staying(n gerund);
    remaining(n gerund);
    # waiting(n gerund);
    pausing(n gerund);
    protecting(n gerund);
    continuing(n gerund);

    # staying(adj qualifier gerund-like);
    remaining(adj qualifier gerund-like);
    # waiting(adj qualifier gerund-like);
    pausing(adj qualifier gerund-like);
    protecting(adj qualifier gerund-like);
    continuing(adj qualifier gerund-like);

e:
    # core

    `[`marks the start of a direct object`]`(particle def);

eliki:
#     # sandbox
#
#     trial(n);
#     adversity(n);
#     bittersweet(adj opinion);

en:
    # core

    `[`separates multiple subjects`]`(particle def);

epiku:
    # uncommon

    # epic(adj opinion);
    # cool(adj opinion); # Could be conflated with "cool" as in lete
    awesome(adj opinion);
    # amazing(adj opinion);

    # coolness(n);
    awesomeness(n);

    amazingly(adv);

esun:
    # core

    # trade(v) [object];
    # barter(v) [object];
    # exchange(v) [object];
    swap(v) [object];
    buy(v) [object];
    sell(v) [object];

    market(n);
    # shop(n);
    # fair(n);
    # bazaar(n);
    business(adj qualifier) place(n);

    # trading(n gerund);
    # bartering(n gerund);
    # exchanging(n gerund);
    swapping(n gerund);
    buying(n gerund);
    selling(n gerund);

    # trading(adj qualifier gerund-like);
    # bartering(adj qualifier gerund-like);
    # exchanging(adj qualifier gerund-like);
    swapping(adj qualifier gerund-like);
    buying(adj qualifier gerund-like);
    sell(adj qualifier);

ijo:
    # core

    # thing(n);
    # object(n);
    entity(n);
    being(n);
    matter(n);
    phenomenon(n);

ike:
    # core

    bad(adj opinion);
    # unpleasant(adj opinion);
    # harmful(adj opinion);
    # unneeded(adj opinion);

    negative(adj opinion) quality(n);

    badly(adv);
    # unpleasantly(adv);
    # harmfully(adv);

ilo:
    # core

    tool(n);
    # implement(n);
    # machine(n);
    # device(n);

    tool-related(adj opinion);
    # implement-related(adj opinion);
    # machine-related(adj opinion);
    # device-related(adj opinion);

insa:
    # core

    inside(adj qualifier) part(n);
    # center(n singular);
    middle(n);
    midpoint(n);

    internal(adj qualifier);

jaki:
    # core

    disgusting(adj opinion);
    unclean(adj opinion);
    # unsanitary(adj opinion);
    # toxic(adj opinion);
    repulsive(adj opinion);
    # rotten(adj opinion);

    grossness(n);
    # toxicity(n);
    # rottenness(n);

    disgustingly(adv);

jan:
    # core

    person(n);
    somebody(n singular);
    human being(n);

    human-like(adj qualifier);
    # person-like(adj qualifier);

    human-related(adj opinion);
    # person-related(adj opinion);

jasima:
    # uncommon

    reflect(v) [object];
    # echo(v) [object];
    # mirror(v) [object];
    duplicate(v) [object];

    reflecting(n gerund);
    # echoing(n gerund);
    # mirroring(n gerund);
    duplicating(n gerund);

    reflecting(adj qualifier gerund-like);
    # echoing(adj qualifier gerund-like);
    # mirroring(adj qualifier gerund-like);
    duplicating(adj qualifier gerund-like);

jelo:
    # core

    yellow(adj color);
    # golden(adj color);
    # lime(adv) yellow(adj color);
    # yellowish(adv) orange(adj color);

    yellow(n singular);
    # lime(adj color) yellow(n singular);
    # yellowish(adj color) orange(n singular);

jo:
    # core

    # hold(v) [object];
    carry(v) [object];
    contain(v) [object];
    own/owns/owned(v) [object];

    # holding(n gerund);
    carrying(n gerund);
    containing(n gerund);
    owning(n gerund);

kala:
    # core

    fish(n);
    # marine(adj qualifier) animal(n);
    # sea(adj qualifier) creature(n);
    swimming(adj qualifier gerund-like) creature(n);

    swim(v);

    fish-like(adj qualifier);
    fish-related(adj opinion);

    swimming(adj qualifier gerund-like);

kalama:
    # core

    produce(v) sound(n);
    sound(n);

    sound-related(adj opinion);

kama:
    # core

    arrive(v);
    # approach(v);
    summon(v) [object];

    future(adj age);

    become(v linking) [predicate];
    begin to(v) [predicate];

    arriving(n gerund);
    # approaching(n gerund);
    summoning(n gerund);
    becoming(n gerund);

    arriving(adj qualifier gerund-like);
    # approaching(adj qualifier gerund-like);
    summoning(adj qualifier gerund-like);
    becoming(adj qualifier gerund-like);

kasi:
    # core

    plant(n);
    # vegetation(n);
    # herb(n);
    leaf(n);

    plant-like(adj qualifier);
    # vegetation-like(adj qualifier);
    # herb-like(adj qualifier);
    leaf-like(adj qualifier);

    plant-related(adj opinion);
    # vegetation-related(adj opinion);
    # herb-related(adj opinion);
    leaf-related(adj opinion);

ken:
    # core

    can(v modal) [predicate];
    may(v modal) [predicate];

    ability(n);
    permission(n);
    possibility(n);

    allow(v) [object];
    enable(v) [object];

kepeken:
    # core

    use(v) [object];

    using(prep) [indirect object];
    by means of(prep) [indirect object];

kijetesantakalu:
    # common

    raccoon(n);
    # kinkajou(n);
    procyonid(n);
    musteloid(n);

    procyonid(adj qualifier);
    musteloid(adj qualifier);

    raccoon-like(adj qualifier);
    # kinkajou-like(adj qualifier);

    raccoon-related(adj opinion);
    # kinkajou-related(adj opinion);

kili:
    # core

    fruit(n);
    vegetable(n);
    mushroom(n);

    fruit-like(adj qualifier);
    vegetable-like(adj qualifier);
    mushroom-like(adj qualifier);

    fruit-related(adj opinion);
    vegetable-related(adj opinion);
    mushroom-related(adj opinion);

kin:
    # common

    `[`at sentence start`]`(particle def);
    also(adv);
    additionally(adv);

kipisi:
    # common

    # split(v) [object];
    cut(v) [object];
    # slice(v) [object];

    piece(n);
    part(n);

    sharp(adj physical quality);
    pointy(adj physical quality);

    # splitting(n gerund);
    cutting(n gerund);
    # slicing(n gerund);

kiwen:
    # core

    hard(adj material) object(n);
    # metal(n);
    # rock(n);
    # stone(n);

    hard(adj material);

ko:
    # core

    semi-solid(n singular);
    # paste(n singular);
    powder(n singular);
    # goo(n singular);
    # sand(n singular);
    # soil(n singular);
    # clay(n singular);

    squishy(adj material);
    moldable(adj material);

    sticky(adj material);

kokosila:
    # obscure

    speak(v) a(d article singular) non-Toki Pona(adj qualifier)
        language(n singular);

kon:
    # core

    air(n);
    # breath(n);
    # wind(n);

    essence(n);
    # spirit(n);
    soul(n);
    # ghost(n);

    unseen(adj physical quality) agent(n);

    air-like(adj qualifier);
    # breath-like(adj qualifier);
    # wind-like(adj qualifier);
    essence-like(adj qualifier);
    # spirit-like(adj qualifier);
    soul-like(adj qualifier);
    # ghost-like(adj qualifier);

    air-related(adj opinion);
    # breath-related(adj opinion);
    # wind-related(adj opinion);
    essence-related(adj opinion);
    # spirit-related(adj opinion);
    soul-related(adj opinion);
    # ghost-related(adj opinion);

ku:
    # common

    interact(v) with(prep) the(d article) Toki Pona(adj qualifier)
        Dictionary(n singular);
    the(d article) Toki Pona(adj qualifier) Dictionary(n);

kule:
    # core

    color(n);
    # pigment(n);
    category(n);
    # genre(n);
    flavor(n);

    colorful(adj color);
    diverse(adj qualifier);

    colorfully(adv);

kulupu:
    # core

    group(n);
    # community(n);
    # society(n);
    # company(n);
    # nation(n);
    # collection(n);
    # team(n);
    # crowd(n);

    group-related(adj opinion);
    # community-related(adj opinion);
    # society-related(adj opinion);
    # company-related(adj opinion);
    # nation-related(adj opinion);
    # collection-related(adj opinion);
    # team-related(adj opinion);
    # crowd-related(adj opinion);

kute:
    # core

    ear(n);
    hearing(adj qualifier gerund-like) organ(n);

    hear(v) [object];
    listen(v) at(prep) [object];
    pay(v) attention(n) to(prep) [object];
    obey(v) [object];

    hearing(n gerund);
    listen(n gerund);
    obeying(n gerund);

    hearing(adj qualifier gerund-like);
    listening(adj qualifier gerund-like);
    obeying(adj qualifier gerund-like);

la:
    # core

    `[`mark the previous statement as context to a following statement`]`
        (particle def);

lanpan:
    # uncommon

    take(v) [object];
    # seize(v) [object];
    # steal(v) [object];

    taking(n gerund);
    # seizing(n gerund);
    # stealing(n gerund);

    taking(adj qualifier gerund-like);
    # seizing(adj qualifier gerund-like);
    # stealing(adj qualifier gerund-like);

lape:
    # core

    sleep(n singular);
    rest(n singular);
    sleep(v);
    rest(v);

    sleeping(adj qualifier gerund-like);
    resting(adj qualifier gerund-like);

laso:
    # core

    # turquoise(adj color);
    blue(adj color);
    green(adj color);
    cyan(adj color);
    # indigo(adj color);
    # lime(adv) green(adj color);

    # turquoise(n singular);
    blue(n singular);
    green(n singular);
    cyan(n singular);
    # indigo(n singular);
    # lime(adj color) green(n singular);

lawa:
    # core

    head(n);
    mind(n);
    brain(n);

    control(v) [object];
    lead(v) [object];
    guide(v) [object];

    # government(n);
    leader(n);

    rule(n);
    law(n);

    head-related(adj opinion);
    mind-related(adj opinion);
    brain-related(adj opinion);
    # government-related(adj opinion);
    leader-related(adj opinion);
    rule-related(adj opinion);
    law-related(adj opinion);

leko:
    # common

    square(n);
    # cube(n);
    block(n);
    blocky(adj physical quality) object(n);
    # bricks(n plural);
    # stairs(n plural);

    square(adj physical quality);
    cuboid(adj physical quality);
    blocky(adj physical quality);

len:
    # core

    # cloth(n);
    # clothing(n);
    fabric(n);
    # textile(n);

    # covered(adj origin);
    hidden(adj origin);
    secret(adj opinion);
    private(adj opinion);

    cover(v) [object];
    hide(v) [object];

    secretly(adv);

lete:
    # core

    cold(adj material);
    # cool(adj material); # Could be conflated with "cool" as in pona
    # frozen(adj material);

    freeze(v) [object];
    # chill(v) [object];

    # raw(adj opinion);
    # uncooked(adj opinion);

    coldness(n);
    # coolness(n);

li:
    # core

    `[`marks the start of an indicative verb `(`statement`)``]`(particle def);

lili:
    # core

    small(adj size);
    short(adj size);
    young(adj age);

    few(d quantifier plural);

    piece(n) of(prep) [headword];
    part(n) of(prep) [headword];

    smallness(n);
    shortness(n);

    slightly(adv);

linja:
    # core

    long(adj size) flexible(adj material) thing(n);
    # rope(n);
    # yarn(n);
    # hair(n);
    # fur(n);
    # line(n);
    # strand(n);

    long(adj size) and(c) flexible(adj material);

lipu:
    # core

    flat(adj physical quality) bendable(adj material) object(n);
    paper(n);
    # card(n);
    # leaf(n);

    written(adj qualifier) text(n);
    # document(n);
    # book(n);
    website(n);
    # clay(adj qualifier) tablet(n);

    flat(adj size) and(c) bendable(adj material);

loje:
    # core

    red(adj color);
    # magenta(adj color);
    # scarlet(adj color);
    # pink(adj color);
    # rust-colored(adj color);
    # reddish(adv) orange(adj color);

    red(n singular);
    # magenta(n singular);
    # scarlet(n singular);
    # pink(n singular);
    # rust-color(n singular);
    # reddish(adj color) orange(n singular);

lon:
    # core

    present(adj origin);
    existing(adj opinion);
    real(adj opinion);
    true(adj opinion);

    located at(prep) [indirect object];
    in(prep) [indirect object];
    during(prep) [indirect object];
    in the context of(prep) [indirect object];

    truth(n singular);

    truthfully(adv);

luka:
    # core

    hand(n);
    arm(n);
    tactile(adj qualifier) organ(n);
    grasping(adj qualifier gerund-like) organ(n);

    grasp(v) [object];
    interact(v) with(prep) [object];
    feel(v) [object] using(prep) touch(n singular);

    5(num);

    hand-like(adj physical quality);
    arm-like(adj physical quality);

    hand-related(adj opinion);
    arm-related(adj opinion);

lukin, oko:
    # core, uncommon

    see(v) [object];
    look(v) at(prep) [object];
    # view(v) at(prep) [object];
    examine(v) [object];
    read(v) [object];
    watch(v) [object];

    visual(adj qualifier);

    eye(n);
    seeing(adj qualifier gerund-like) organ(n);

    try to(v) [predicate];

lupa:
    # core

    hole(n);
    # pit(n);
    # cave(n);
    # doorway(n);
    # window(n);
    # portal(n);

    holey(adj physical quality);

    hole-related(adj opinion);

ma:
    # core

    earth(n singular);
    land(n singular);
    soil(n singular);

    # country(n);
    territory(n);
    world(n singular);

    outdoor(n);

    earth-related(adj opinion);
    land-related(adj opinion);
    soil-related(adj opinion);
    # country-related(adj opinion);
    territory-related(adj opinion);
    world-related(adj opinion);
    outdoor-related(adj opinion);

majuna:
    # uncommon

    old(adj age);
    # aged(adj age);
    ancient(adj age);

    oldness(n);

mama:
    # core

    parent(n);
    ancestor(n);

    creator(n);
    # originator(n);

    caretaker(n);
    sustainer(n);
    guardian(n);

    parent-related(adj opinion);
    ancestor-related(adj opinion);
    creator-related(adj opinion);
    # originator-related(adj opinion);
    caretaker-related(adj opinion);
    sustainer-related(adj opinion);
    guardian-related(adj opinion);

mani:
    # core

    money(n singular);
    # currency(n);
    valuable(adj opinion) thing(n);

    # gold(n);
    investment(n);
    livestock(n);

    valuable(adj opinion);

meli:
    # common

    woman(n);
    feminine(adj opinion);
    wife(n);
    girlfriend(n);

meso:
    # uncommon

    midpoint(n);
    medium(adj size);
    mediocre(adj opinion);

mi:
    # core

    I/me/we/us(personal pronoun first);
    my/our(d possessive);

mije:
    # common

    man(n);
    masculine(adj qualifier);
    husband(n);
    boyfriend(n);

misikeke:
    # common

    medical(adj qualifier) item(n);
    # prescription(n);
    meditation(n);
    exercise(n);
    # bandage(n);
    therapy(n);

    medical(adj qualifier);

moku:
    # core

    eat(v) [object];
    drink(v) [object];
    consume(v) [object];
    # swallow(v) [object];
    # ingest(v) [object];

    food(n);
    edible(adj opinion) thing(n);

    food-related(adj opinion);

moli:
    # core

    death(n singular);
    dead(adj age);
    die(v);
    # dying(n);
    kill(v) [object];
    # murder(v) [object];

monsi:
    # core

    back(n singular);
    rear(n singular);

    back-related(adj opinion);
    rear-related(adj opinion);

monsuta:
    # common

    fear(n);
    # nervousness(n);
    dread(n);

    scary(adj opinion);
    # frightening(adj opinion);

    scary(adj opinion) thing(n);
    # predator(n);
    # threat(n);
    danger(n);

mu:
    # core

    bark(v);
    cough(v);
    roar(v);
    hum(v);
    quack(v);
    hiss/hisses/hissed(v);
    buzz(v);
    growl(v);
    yawn(v);
    screech(v);
    chirp(v);
    gobble(v);
    purr(v);
    honk(v);
    burp(v);
    chomp(v);

    bark(i);
    cough(i);
    roar(i);
    hum(i);
    ow(i);
    quack(i);
    hiss(i);
    buzz(i);
    growl(i);
    yawn(i);
    woof(i);
    screech(i);
    chirp(i);
    hoot(i);
    moo(i);
    hiccup(i);
    gobble(i);
    purr(i);
    baa(i);
    honk(i);
    tweet(i);
    ouch(i);
    meow(i);
    burp(i);
    chomp(i);
    ribbit(i);
    achoo(i);
    dook(i); # nano li wile e ni

    barking(n gerund);
    coughing(n gerund);
    roaring(n gerund);
    humming(n gerund);
    quacking(n gerund);
    hissing(n gerund);
    buzzing(n gerund);
    growling(n gerund);
    yawning(n gerund);
    screeching(n gerund);
    chirping(n gerund);
    gobbling(n gerund);
    purring(n gerund);
    honking(n gerund);
    burping(n gerund);
    chomping(n gerund);

mun:
    # core

    # moon(n singular);
    night sky(adj qualifier) object(n);
    # star(n);
    celestial(adj qualifier) body(n);

    celestial(adj qualifier);

    moon-related(adj opinion);
    star-related(adj opinion);

musi:
    # core

    # fun(n singular);
    game(n);
    entertainment(n);

    art(n);
    fun(adj opinion);
    amusing(adj opinion gerund-like);
    # interesting(adj opinion gerund-like);
    comical(adj opinion);
    # silly(adj opinion);

    have(v) fun(n singular);

    amusingly(adv);
    comically(adv);

mute:
    # core

    many(d quantifier plural);
    # several(d quantifier plural);
    very(adv);
    20(num);

    quantity(n);

n:
    # common

    hm/hmm/hmmm(f);
    # uh/uhh/uhhh(f);
    # mm/mmm/mmmm(f);
    # er/err/errr(f);
    # um/umm/ummm(f);
    `[`indicate thinking or pause`]`(particle def);

namako:
    # common

    spice(n);
    ornament(n);
    # adornment(n);

    extra(adj opinion);
    additional(adj opinion);

nanpa:
    # core

    number(n);
    -th(particle def);
    `[`ordinal number`]`(particle def);

    numeric(adj opinion);

nasa:
    # core

    unusual(adj opinion);
    # strange(adj opinion);
    silly(adj opinion);
    # drunk(adj opinion);
    intoxicated(adj opinion);

    intoxicate(v) [object];

    strangely(adv);

    silliness(n singular);
    strangeness(n singular);

nasin:
    # core

    method(n);
    doctrine(n);
    tradition(n);

    path(n);
    # road(n);
    # way(n);

    method-related(adj opinion);
    doctrine-related(adj opinion);
    tradition-related(adj opinion);
    path-related(adj opinion);
    # road-related(adj opinion);
    # way-related(adj opinion);

nena:
    # core

    protuberance(n);
    bump(n);
    button(n);
    hill(n);
    nose(n);

    protuberance-related(adj opinion);
    bump-related(adj opinion);
    button-related(adj opinion);
    hill-related(adj opinion);
    nose-related(adj opinion);

ni:
    # core

    this/these(n);
    that/those(n);
    this/these(d demonstrative);
    that/those(d demonstrative);

nimi:
    # core

    word(n);
    name(n);

    word-related(adj opinion);
    name-related(adj opinion);

noka:
    # core

    foot(n);
    leg(n);
    locomotive(adj qualifier) organ(n);
    root(n);

    foot-like(adj physical quality);
    leg-like(adj physical quality);
    root-like(adj physical quality);

    foot-related(adj opinion);
    leg-related(adj opinion);
    root-related(adj opinion);

o:
    # core

    `[`marks the end of a vocative `(`who is being spoken to`)``]`
        (particle def);
    `[`marks the start of an imperative `(`command, wish, instruction`)``]`
        (particle def);
    should(particle def);

olin:
    # core

    have(v) strong(adj opinion) emotional(adj opinion) bond(n singular)
        with(prep) [object];
    respect(v) [object];

    affection(n singular);
    appreciation(n singular);
    respect(n singular);
    relationship(n);
    platonic(adj qualifier);
    romantic(adj qualifier);
    familial(adj qualifier);

    respectfully(adv);
    platonically(adv);
    romantically(adv);

ona:
    # core

    they/them(personal pronoun third plural);
    it/it(personal pronoun third singular);
    their(d possessive);
    its(d possessive);

open:
    # core

    # begin(v);
    start(v);
    open(v) [object];
    turn on(v) [object];

    beginning(n);

    beginning-related(adj opinion);

pakala:
    # core

    damaged(adj opinion);
    # broken(adj opinion);
    # botched(adj opinion);
    # harmed(adj opinion);
    # messed up(adj opinion);

    mistake(n);

    damage(v) [object];
    # break(v) [object];
    # botch(v) [object];
    # harm(v) [object];
    # mess up(v) [object];

    incorrectly(adv);

pali:
    # core

    # work(n);
    activity(n);

    create(v) [object];
    # build(v) [object];
    design(v) [object];

    put(v) effort(n) toward(prep) [object];
    take(v) action(n) to(prep) [object];

    # work-related(adj opinion);
    activity-related(adj opinion);

palisa:
    # core

    long(adj size) hard(adj material) thing(n);
    # branch(n);
    # pole(n);
    # rod(n);
    # stick(n);
    # spine(n);
    # mast(n);

    long(adj size) and(c) hard(adj material);

pan:
    # core

    grain(n);
    starchy(adj material) food(n);
    baked(adj qualifier) goods(n plural);

    # rice(n);
    # sorghum(n);
    # bread(n);
    # noodle(n);
    # masa(n);
    # porridge(n);
    # injera(n);

    starchy(adj material);
    grain-related(adj opinion);

pana:
    # core

    give(v) [object];
    # send(v) [object];
    emit(v) [object];
    provide(v) [object];
    put(v) [object];
    release(v) [object];

    giving(n gerund);
    # sending(n gerund);
    emitting(n gerund);
    providing(n gerund);
    putting(n gerund);
    releasing(n gerund);

    giving(adj qualifier gerund-like);
    # sending(adj qualifier gerund-like);
    emitting(adj qualifier gerund-like);
    providing(adj qualifier gerund-like);
    putting(adj qualifier gerund-like);
    releasing(adj qualifier gerund-like);

pi:
    # core

    `[`modify the next word with one or more following words`]`(particle def);

pilin:
    # core

    experience(n);
    emotion(n);
    # feeling(n);
    touch(n);

    feel(v) [object];
    touch(v) [object];

    heart(n);

    experience-related(adj opinion);
    emotion-related(adj opinion);
    # feeling-related(adj opinion);
    touch-related(adj opinion);
    heart-related(adj opinion);

pimeja:
    # core

    dark(adj color);
    unlit(adj color);

    dark(adj color) color(n);
    # black(adj color);
    # purple(adj color);
    # brown(adj color);

    darkness(n singular);

pini:
    # core

    finish(v) [object];
    stop(v) [object];
    prevent(v) [object];

    close(v) [object];
    disable(v) [object];
    turn off(v) [object];

    ended(adj age);
    past(adj age);

    edge(n);
    end(n);
    conclusion(n);

pipi:
    # core

    insect(n);
    bug(n);
    # spider(n);
    tiny(adj size) crawling(adj qualifier gerund-like) creature(n);

    insect-like(adj physical quality);
    bug-like(adj physical quality);

    insect-related(adj opinion);
    bug-related(adj opinion);

poka:
    # core

    hip(n);
    side(n);
    vicinity(n);
    nearby(adj origin);

poki:
    # core

    container(n);
    # bag(n);
    # bowl(n);
    # box(n);
    # cup(n);
    # cupboard(n);
    # drawer(n);
    # folder(n);

    container-like(adj physical quality);
    container-related(adj opinion);

pona:
    # core

    good(adj opinion);
    # pleasant(adj opinion);
    # helpful(adj opinion);
    # friendly(adj opinion);
    # useful(adj opinion);
    # peaceful(adj opinion);

    positive(adj opinion) quality(n);

    nicely(adv);
    # TODO: well(adv); # Implement this when ilo Token can add adverbs after the verbs or adjectives

pu:
    # core

    interact(v) with(prep) the(d article) book(n singular) titled(adj)
        Toki Pona`:` The Language of Good(n proper);
    the(d article) book(n) titled(adj)
        Toki Pona`:` The Language of Good(n proper);

sama:
    # core

    same(adj opinion);
    # similar(adj opinion);
    # alike(adj opinion);

    fellow(adj opinion);

    similar to(prep) [indirect object];
    # same as(prep) [indirect object];

    similarity(n);

    equally(adv);

seli:
    # core

    hot(adj material);
    warm(adj material);

    heat(n);
    fire(n);
    # flame(n);

    burn(v) [object];

selo:
    # core

    outer(adj origin) layer(n);
    # skin(n singular);
    # peel(n singular);
    # shell(n singular);
    # bark(n singular);

    # outer(adj origin) shape(n);
    outer(adj origin) form(n);
    boundary(n);

    skin-related(adj opinion);
    boundary-related(adj opinion);

seme:
    # core

    `[`indicate a question by marking missing info in a sentence`]`
        (particle def);
    what/what(n);
    which/which(n);
    what(d interrogative);
    which(d interrogative);

sewi:
    # core

    # top(adj origin);
    above(adj origin);
    highest(adj origin) part(n);

    divine(adj opinion);
    sacred(adj opinion);
    supernatural(adj opinion);

    awesome(adj opinion);
    inspiring(adj opinion);
    excelling(adj opinion);

    elevate(v) [object];

sijelo:
    # core

    body(n);
    shape(n);
    physical(adj qualifier) state(n);
    # torso(n);
    substance(n);
    form(n);

    body-related(adj opinion);
    shape-related(adj opinion);
    # torso-related(adj opinion);
    substance-related(adj opinion);
    form-related(adj opinion);

sike:
    # core

    circle(n);
    sphere(n);
    spiral(n);
    round(adj physical quality) thing(n);
    # ball(n);
    # wheel(n);

    repeating(adj qualifier gerund-like) thing(n);
    cycle(n);
    orbit(n);
    loop(n);

    round(adj physical quality);
    repeating(adj qualifier gerund-like);

    repeatedly(adv);

sin:
    # core

    new(adj age);
    fresh(adj opinion);

    update(v) [object];

    newness(n singular);

    repeatedly(adv);
    newly(adv);

sina:
    # core

    you/you(personal pronoun second plural);
    your(d possessive);

sinpin:
    # core

    vertical(adj physical quality) surface(n);
    # wall(n);
    # board(n);

    front(n);
    face(n);

    wall-related(adj opinion);
    # board-related(adj opinion);
    front-related(adj opinion);
    face-related(adj opinion);

sitelen:
    # core

    image(n);
    # picture(n);
    representation(n);
    symbol(n);
    # mark(n);
    # writing(n);

    image-related(adj opinion);
    # picture-related(adj opinion);
    representation-related(adj opinion);
    symbol-related(adj opinion);
    # mark-related(adj opinion);
    # writing-related(adj opinion);

soko:
    # common

    mushroom(n);
    fungus(n);
    lichen(n);

    mushroom-like(adj physical quality);
    fungus-like(adj physical quality);
    lichen-like(adj physical quality);

    mushroom-related(adj opinion);
    fungus-related(adj opinion);
    lichen-related(adj opinion);

sona:
    # core

    knowledge(n);
    information(n);
    data(n plural); # data is not singular, datum sounds unfamiliar

    know(v) [object];
    have(v) skill(n singular) in(prep) [object];
    # have(v) knowledge(n singular) about(prep) [object];

    know how to/knows how to/knew how to(v) [predicate];

    knowledgeable(adj opinion);

    knowledge-related(adj opinion);
    information-related(adj opinion);
    data-related(adj opinion);

soweli:
    # core

    fuzzy(adj material) creature(n);
    land(adj qualifier) animal(n);
    beast(n);

    animal-like(adj physical quality);
    beast-like(adj physical quality);

    animal-related(adj opinion);
    beast-related(adj opinion);

su:
    # uncommon

    interact(v) with(prep) Mrs. Lang's(d possessive) story(adj qualifier)
        book(adj qualifier) series(n singular);
    Mrs. Lang's(d possessive) story(adj qualifier) book(adj qualifier) series(n);

suli:
    # core

    big(adj size);
    heavy(adj size);
    # large(adj size);
    long(adj size);
    tall(adj size);
    wide(adj size);

    important(adj opinion);
    relevant(adj opinion);

    hugeness(n singular);
    importance(n singular);

suno:
    # core

    # light(v);
    shine(v);
    # glow(v);

    light(n);
    radiance(n);

    # sun(n);
    light(adj qualifier) source(n);
    # brightness(n);

    light-related(adj opinion);

supa:
    # core

    horizontal(adj physical quality) surface(n);
    # bed(n);
    # floor(n);
    # desk(n);
    # plate(n);
    # table(n);
    # platform(n);

    horizontal(adj physical quality);

suwi:
    # core

    sweet(adj material);
    fragrant(adj material);
    cute(adj opinion);
    # adorable(adj opinion);

    sweetness(n);
    fragrance(n);
    cuteness(n);

    charmingly(adv);

tan:
    # core

    from(prep) [indirect object];
    because of(prep) [indirect object];

    origin(n);
    cause(n);

taso:
    # core

    only(adj opinion);
    only(adv);
    `[`marks a sentence as qualifying or contradictory`]`(particle def);
    but(particle def);
    however(particle def);

tawa:
    # core

    motion(n);
    # walk(v);
    shake(v) [object];
    fly(v);
    travel(v);

    # walking(n gerund);
    shaking(n gerund);
    flight(n);
    traveling(n gerund);

    to(prep) [indirect object];
    for(prep) [indirect object];
    going to(prep) [indirect object];
    from the perspective of(prep) [indirect object];

    moving(adj physical quality);

telo:
    # core

    liquid(n singular);
    # water(n singular);
    # gasoline(n singular);
    # soda(n singular);
    # lava(n singular);
    # soup(n singular);
    # oil(n singular);
    # ink(n singular);

    liquid(adj material);

tenpo:
    # core

    time(n singular);
    # event(n);
    # situation(n);
    # moment(n);
    # period(n);
    # duration(n singular);

    time-related(adj opinion);
    # event-related(adj opinion);
    # situation-related(adj opinion);
    # moment-related(adj opinion);
    # period-related(adj opinion);
    # duration-related(adj opinion);

toki:
    # core

    communicate(v) about(prep) [object];
    # say(v) [object];
    think(v) about(prep) [object];

    conversation(n);
    story(n);

    language(n);

    communicating(adj qualifier gerund-like);
    # saying(adj qualifier gerund-like);
    thinking(adj qualifier gerund-like);

    conversation-related(adj opinion);
    story-related(adj opinion);
    language-related(adj opinion);

tomo:
    # core

    indoor(adj qualifier) space(n);
    # shelter(n);
    # room(n);
    building(n);
    # home(n);
    # tent(n);
    # shack(n);

    # shelter-related(adj opinion);
    # room-related(adj opinion);
    building-related(adj opinion);
    # home-related(adj opinion);
    # tent-related(adj opinion);
    # shack-related(adj opinion);

tonsi:
    # common

    non-binary(adj qualifier) person(n);
    gender nonconforming(adj qualifier) person(n);
    gender-queer(adj qualifier) person(n);
    transgender(adj qualifier) person(n);

    non-binary(adj qualifier);
    gender nonconforming(adj qualifier);
    gender-queer(adj qualifier);
    transgender(adj qualifier);

tu:
    # core

    2(num);
    separate(v) [object];
    divide(v) [object];
    split(v) [object];

    separating(n gerund);
    dividing(n gerund);
    splitting(n gerund);

    separated(adj opinion);
    divided(adj opinion);
    split(adj opinion);

unpa:
    # core

    sexual(adj qualifier) activity(n);
    have(v) sex(n singular) with(prep) [object];

    sexual(adj qualifier);

    sexually(adv);

uta:
    # core

    mouth(n);
    lip(n);
    throat(n);
    consuming(adj qualifier gerund-like) orifice(n);

    mouth-related(adj opinion);
    lips-related(adj opinion);
    throat-related(adj opinion);

utala:
    # core

    # fight(v) against(prep) [object];
    compete(v) against(prep) [object];
    # battle(v) against(prep) [object];

    competition(n);
    challenge(n);

    struggle(v);
    strive(v);

    # fight-related(adj opinion);
    competition-related(adj opinion);
    challenge-related(adj opinion);

walo:
    # core

    light-colored(adj color);
    # white(adj color);
    # pale(adj color);
    # light(adv) gray(adj color);
    # cream-colored(adj color);

    light(adj color) color(n);
    # white(n);
    # paleness(n);
    # light(adj color) gray(n);
    # cream(adj qualifier) color(n);

wan:
    # core

    1(num);
    singular(adj opinion);

    combine(v) [object];
    # join(v) [object];
    # mix(v) [object];
    # fuse(v) [object];

waso:
    # core

    bird(n);
    flying(adj qualifier gerund-like) creature(n);
    winged(adj qualifier) animal(n);

    fly(v);

    bird-like(adj physical quality);
    bird-related(adj physical quality);

wawa:
    # core

    power(n);
    # energy(n);
    # strength(n);

    confident(adj opinion);
    intense(adj opinion);
    # forceful(adj opinion);

    amazing(adj opinion);
    # impressive(adj opinion);

    powerfully(adv);

weka:
    # core

    absent(adj origin);
    away(adj origin);
    distant(adj origin);

    remove(v) [object];
    # get rid of

    absence(n);
    removal(n);

wile:
    # core

    want(v) [object];
    # desire(v) [object];
    # wish(v) for(prep) [object];
    require(v) [object];

    want(n);
    # desire(n);
    # wish(n);
    requirement(n);

    want to(v) [predicate];

    want-related(adj opinion);
    # desire-related(adj opinion);
    # wish-related(adj opinion);
    requirement-related(adj opinion);
